import 'package:flutter/material.dart';
import 'package:netflex_clone/widgets/stack_container.dart';

class ProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: StackContainer(),
    );
  }
}
